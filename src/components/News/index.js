import React from "react";

import "./News.scss";

const News = (props) => {
    return (
        <main className={`content dialogs-page`}>
            <div className="content-wrapp">

                News

            </div>
        </main>
    );
};

export default News;
